rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Denegar todo acceso directo de clientes; el acceso se realiza a través de Next.js Server con Firebase Admin SDK
    match /{document=**} {
      allow read, write: if false;
    }
  }
}

rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Los comprobantes son estrictamente privados; sólo accesibles vía Next.js Server autenticado
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
